# Contribuindo

1. Crie uma branch a partir de `main`.
2. Execute `npm run ci` antes de abrir o pull request.
3. Não inclua ROMs, artworks, senhas ou `server/config.json`.
4. Use commits no padrão Conventional Commits.
